function preXhr(e, settings) {
    var API = new $.fn.dataTable.Api(settings);

    App.blockUI({
        message: API.i18n('processing'),
        target: API.table().container(),
        overlayColor: 'none',
        cenrerY: true,
        boxed: true
    });
}
